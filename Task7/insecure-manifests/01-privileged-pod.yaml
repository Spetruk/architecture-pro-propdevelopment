apiVersion: v1
kind: Pod
metadata:
  name: pod-privileged
  namespace: audit-zone
  labels:
    app: insecure-privileged
spec:
  containers:
    - name: nginx
      image: nginx:1.21
      securityContext:
        privileged: true
        runAsUser: 0
      ports:
        - containerPort: 80